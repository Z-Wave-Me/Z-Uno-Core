
# Arduino ZUNO Core and platform.
# ------------------------------

# For more info:
# https://github.com/arduino/Arduino/wiki/Arduino-IDE-1.5---3rd-party-Hardware-specification

name=Arduino ZUNO Boards
version=1.6.1

# ZUNO compile variables
# ---------------------
#sdcc options '-mmcs51 --out-fmt-ihx --model-large --verbose --debug --use-stdout --code-loc 0x8000 --xram-loc 0x3000'

#Windows x86
tools.zprog.upload.pattern={runtime.ide.path}\hardware\tools\zuno\compiler.exe prog "{build.path}\{build.project_name}" -p "{runtime.ide.path}\hardware\tools\sdcc\bin\" -d {serial.port} -r {runtime.ide.path} -o '-mmcs51 --out-fmt-ihx --model-large --verbose --debug --use-stdout --code-loc 0x8000 --xram-loc 0x3000'

# Default "compiler.path" is correct, change only if you want to overidde the initial value
compiler.path={runtime.ide.path}\hardware\tools\zuno\

# ZUNO compile patterns
# --------------------

recipe.c.o.pattern=
recipe.cpp.o.pattern={runtime.ide.path}\hardware\tools\zuno\compiler.exe size "{build.path}\{build.project_name}" -r {runtime.ide.path} -p {runtime.ide.path}\hardware\tools\sdcc\bin\ -o '-mmcs51 --out-fmt-ihx --model-large --verbose --debug --use-stdout --code-loc 0x8000 --xram-loc 0x3000'
recipe.S.o.pattern=
recipe.ar.pattern=
recipe.c.combine.pattern=
recipe.objcopy.eep.pattern=
recipe.objcopy.hex.pattern=

## Compute size
recipe.size.pattern=
recipe.size.regex=
recipe.size.regex.data=
recipe.size.regex.eeprom=

# ZUNO Uploader/Programmers tools
# ------------------------------
tools.zprog.config.path=

upload.params.verbose=
tools.zprog.upload.params.verbose=
tools.zprog.upload.params.quiet=

tools.zprog.program.params.verbose=
tools.zprog.program.params.quiet=
tools.zprog.program.pattern=

# USB Default Flags
# Default blank usb manufacturer will be filled it at compile time
# - from numeric vendor ID, set to Unknown otherwise
build.usb_manufacturer="Unknown"
build.usb_flags=-DUSB_VID={build.vid} -DUSB_PID={build.pid} '-DUSB_MANUFACTURER={build.usb_manufacturer}' '-DUSB_PRODUCT={build.usb_product}'
